- name: Install qemu
  hosts: testttt
  become: true
  tasks:
    - name: Install qemu guest agent
      ansible.builtin.package:
        name: qemu-guest-agent
        state: present

    - name: Reboot
      ansible.builtin.reboot:
